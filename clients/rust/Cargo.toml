[package]
name = "zmr-client"
version = "0.2.1"
edition = "2021"
license = "MIT"
description = "Rust JSON-RPC client for Zeno Mobile Runner."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
